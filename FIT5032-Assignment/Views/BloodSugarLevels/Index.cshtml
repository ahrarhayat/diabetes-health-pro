@model IEnumerable<FIT5032_Assignment.Models.BloodSugarLevel>

@{
   
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="" style="background-color: white; opacity: 85%; color: black; width: 100%;height:200%">
    <h2>View all your recorded blood sugar levels</h2>

    <p>
        @Html.ActionLink("Record new blood sugar level", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BloodSugarLevel1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfTest)
            </th>
            @* <th>
                    @Html.DisplayNameFor(model => model.User_Id)
                </th>*@
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.BloodSugarLevel1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateOfTest)
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.User_Id)
                    </td>*@
                @*<td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                        @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                    </td>*@
            </tr>
        }

    </table>

    </div>
